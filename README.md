# StemWave 

A lightweight GUI for separating audio stems using Demucs.

## Requirements

- Python 3.9 or later
- Demucs installed globally
- FFmpeg (Demucs uses it internally)

Install Demucs with:

```bash
pip install demucs
